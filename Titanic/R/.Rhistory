install.packages("doSNOW")
install.packages("infotheo")
install.packages("Rtsne")
getwd()
setwd("Kaggle")
setwd("Titanic")
setwd("R")
getwd()
dir()
edit( "titanic.Rmd")
installed.packages()
install.packages("assertthat")
? strsplit
source = "listOfVictims.txt";
fid = file(source,"r");
lastLine = "";
while( TRUE ){
thisLine = readLines(fid, n=1);
tokens = strsplit( thisLine, "\s");
if( length( tokens ) > 1 ){
firstToken = tokens[1];
lastName = gsub( ",$", "", firstToken );
if ( lastName == toupper( lastName )){
# must be last name!
cat( "lastName = ", lastName );
}else{
# not last name!  Concatenate last Line with this line
cat( "NOT lastname: ", thisLine );
}
}
if ( length(thisLine == 0 ) ){
break;
}
}
source = "listOfVictims.txt";
fid = file(source,"r");
getwd()
dir()
source = "C:/Users/nk_wh/Documents/Kaggle/Titanic/listOfVictims.txt";
fid = file(source,"r");
lastLine = "";
while( TRUE ){
thisLine = readLines(fid, n=1);
tokens = strsplit( thisLine, "\s");
if( length( tokens ) > 1 ){
firstToken = tokens[1];
lastName = gsub( ",$", "", firstToken );
if ( lastName == toupper( lastName )){
# must be last name!
cat( "lastName = ", lastName );
}else{
# not last name!  Concatenate last Line with this line
cat( "NOT lastname: ", thisLine );
}
}
if ( length(thisLine == 0 ) ){
break;
}
}
while( TRUE ){
thisLine = readLines(fid, n=1);
tokens = strsplit( thisLine, "\s");
if( length( tokens ) > 1 ){
firstToken = tokens[1];
lastName = gsub( ",$", "", firstToken );
if ( lastName == toupper( lastName ) ){
# must be last name!
cat( "lastName = ", lastName );
}else{
# not last name!  Concatenate last Line with this line
cat( "NOT lastname: ", thisLine );
}
}
if ( length(thisLine) == 0 ){
break;
}
}
thisLine = readLines(fid, n=1);
tokens = strsplit( thisLine, "\s");
tokens = strsplit( thisLine, '\s');
while( TRUE ){
thisLine = readLines(fid, n=1);
tokens = strsplit( thisLine, "\\s");
if( length( tokens ) > 1 ){
firstToken = tokens[1];
lastName = gsub( ",$", "", firstToken );
if ( lastName == toupper( lastName ) ){
# must be last name!
cat( "lastName = ", lastName );
}else{
# not last name!  Concatenate last Line with this line
cat( "NOT lastname: ", thisLine );
}
}
if ( length(thisLine) == 0 ){
break;
}
}
fid = file(source,"r");
lastLine = "";
thisLine = readLines(fid, n=1);
tokens = strsplit( thisLine, "\\s");
tokens
if( length( tokens ) > 1 ){
firstToken = tokens[1];
lastName = gsub( ",$", "", firstToken );
if ( lastName == toupper( lastName ) ){
# must be last name!
cat( "lastName = ", lastName );
}else{
# not last name!  Concatenate last Line with this line
cat( "NOT lastname: ", thisLine );
}
}
tokens
length(tokens)
str(tokens)
summary(tokens)
tokens
tokens[2]
tokens[1]
tokens[1][1]
tokens[[1]]
tokens[[2]]
quit()
